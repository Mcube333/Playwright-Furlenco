# ---- Production Environment Configuration ----
# WARNING: Only smoke / read-only / production-validation suites should ever target this file.
# Never run destructive or data-mutating regression suites against prod.

web.base.url=https://www.example.com
browser=chromium
headless=true
default.timeout.ms=45000
slowmo.ms=0

api.base.url=https://api.example.com/api

test.user.username=
test.user.password=

db.host=
db.port=5432
db.name=
db.user=
db.password=
db.pool.size=2

api.token=

retry.max.count=1
thread.count=2
